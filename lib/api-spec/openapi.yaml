openapi: 3.1.0
info:
  title: Api
  version: 0.1.0
  description: EcoLearn Mauritius API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
  - name: courses
  - name: categories
  - name: enrollments
  - name: progress
  - name: quizzes
  - name: certificates
  - name: companies
  - name: employees
  - name: plans
  - name: subscriptions
  - name: dashboard
  - name: impact
  - name: blog
  - name: testimonials
  - name: leads
  - name: admin

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  # ── COURSES ──────────────────────────────────────────────────────────────────
  /courses:
    get:
      operationId: listCourses
      tags: [courses]
      summary: List all published courses
      parameters:
        - name: categoryId
          in: query
          schema:
            type: ["integer", "null"]
        - name: search
          in: query
          schema:
            type: ["string", "null"]
        - name: featured
          in: query
          schema:
            type: ["boolean", "null"]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Course"
    post:
      operationId: createCourse
      tags: [courses]
      summary: Create a course (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CourseInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Course"

  /courses/{id}:
    get:
      operationId: getCourse
      tags: [courses]
      summary: Get a single course with lessons and objectives
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseDetail"
    patch:
      operationId: updateCourse
      tags: [courses]
      summary: Update a course (admin)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CourseUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Course"
    delete:
      operationId: deleteCourse
      tags: [courses]
      summary: Delete a course (admin)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /courses/featured:
    get:
      operationId: getFeaturedCourses
      tags: [courses]
      summary: Get featured courses for the homepage
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Course"

  # ── CATEGORIES ────────────────────────────────────────────────────────────────
  /categories:
    get:
      operationId: listCategories
      tags: [categories]
      summary: List all course categories
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Category"

  # ── ENROLLMENTS ───────────────────────────────────────────────────────────────
  /enrollments:
    get:
      operationId: listEnrollments
      tags: [enrollments]
      summary: List enrollments for the current user
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Enrollment"
    post:
      operationId: createEnrollment
      tags: [enrollments]
      summary: Enroll in a course
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EnrollmentInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Enrollment"

  /enrollments/{id}:
    get:
      operationId: getEnrollment
      tags: [enrollments]
      summary: Get a single enrollment with progress
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnrollmentDetail"

  # ── PROGRESS ──────────────────────────────────────────────────────────────────
  /progress/{enrollmentId}:
    get:
      operationId: getProgress
      tags: [progress]
      summary: Get lesson-by-lesson progress for an enrollment
      parameters:
        - name: enrollmentId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LessonProgress"
    patch:
      operationId: updateProgress
      tags: [progress]
      summary: Mark a lesson complete / update progress
      parameters:
        - name: enrollmentId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProgressUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonProgress"

  # ── QUIZZES ───────────────────────────────────────────────────────────────────
  /courses/{courseId}/quiz:
    get:
      operationId: getCourseQuiz
      tags: [quizzes]
      summary: Get quiz questions for a course
      parameters:
        - name: courseId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Quiz"

  /courses/{courseId}/quiz/submit:
    post:
      operationId: submitQuiz
      tags: [quizzes]
      summary: Submit quiz answers
      parameters:
        - name: courseId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuizSubmission"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuizResult"

  # ── COMMITMENTS ───────────────────────────────────────────────────────────────
  /courses/{courseId}/commitments:
    get:
      operationId: getCommitments
      tags: [commitments]
      summary: Get the current user's saved commitments for a course
      parameters:
        - name: courseId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommitmentList"
    post:
      operationId: saveCommitments
      tags: [commitments]
      summary: Save the current user's commitments for a course
      parameters:
        - name: courseId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveCommitmentsInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommitmentList"

  # ── PROGRESSION ───────────────────────────────────────────────────────────────
  /progression/points:
    get:
      operationId: getMyPoints
      tags: [progression]
      summary: Get the current user's total learning points
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PointsBreakdown"

  /progression/courses/{courseId}/summary:
    get:
      operationId: getCourseProgressSummary
      tags: [progression]
      summary: Get the current user's progression summary for a course
      parameters:
        - name: courseId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseProgressSummary"

  # ── CERTIFICATES ──────────────────────────────────────────────────────────────
  /certificates:
    get:
      operationId: listCertificates
      tags: [certificates]
      summary: List certificates for the current user
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Certificate"

  /certificates/company/list:
    get:
      operationId: listCompanyCertificates
      tags: [certificates]
      summary: List all certificates issued across the company
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Certificate"

  /certificates/{id}:
    get:
      operationId: getCertificate
      tags: [certificates]
      summary: Get a certificate
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Certificate"

  /certificates/verify/{code}:
    get:
      operationId: verifyCertificate
      tags: [certificates]
      summary: Publicly verify a certificate by its unique code
      parameters:
        - name: code
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Certificate"

  # ── LEARNING PATHS ────────────────────────────────────────────────────────────
  /learning-paths:
    get:
      operationId: listLearningPaths
      tags: [learning-paths]
      summary: List structured learning paths with the current user's progress
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LearningPath"

  # ── BADGES ────────────────────────────────────────────────────────────────────
  /badges:
    get:
      operationId: listAchievementBadges
      tags: [badges]
      summary: List achievement badges with the current user's earned status
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AchievementBadge"

  /leaderboards:
    get:
      operationId: listLeaderboards
      tags: [leaderboards]
      summary: Company leaderboards (top learners, champions, courses, scores)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaderboardResponse"

  /challenges:
    get:
      operationId: listChallenges
      tags: [challenges]
      summary: Monthly sustainability challenges with the current user's participation
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChallengeListResponse"

  /challenges/{id}/join:
    post:
      operationId: joinChallenge
      tags: [challenges]
      summary: Join a challenge
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
        "201":
          description: Joined

  /challenges/{id}/progress:
    post:
      operationId: logChallengeProgress
      tags: [challenges]
      summary: Log progress towards a challenge goal
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChallengeProgressInput"
      responses:
        "200":
          description: OK

  # ── COMPLIANCE ────────────────────────────────────────────────────────────────
  /compliance/overview:
    get:
      operationId: getComplianceOverview
      tags: [compliance]
      summary: Training compliance dashboard data
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ComplianceOverview"

  /compliance/assignments:
    post:
      operationId: assignCourse
      tags: [compliance]
      summary: Assign a course to employees or a whole department
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignCourseInput"
      responses:
        "201":
          description: Assigned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssignCourseResult"

  /compliance/reminders:
    get:
      operationId: listReminders
      tags: [compliance]
      summary: Recent training reminders
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TrainingReminder"
    post:
      operationId: sendReminder
      tags: [compliance]
      summary: Send a reminder or retraining notification to an employee
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendReminderInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrainingReminder"

  /compliance/employees/bulk-import:
    post:
      operationId: bulkImportEmployees
      tags: [compliance]
      summary: Bulk import employees from parsed spreadsheet rows
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkImportInput"
      responses:
        "201":
          description: Imported
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkImportResult"

  /compliance/run-retraining-scan:
    post:
      operationId: runRetrainingScan
      tags: [compliance]
      summary: Automatically generate retraining notices for all expired training
      responses:
        "201":
          description: Notices generated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RetrainingScanResult"

  # ── ADMIN ─────────────────────────────────────────────────────────────────────
  /admin/analytics:
    get:
      operationId: getAdminAnalytics
      tags: [admin]
      summary: Platform-wide analytics for the super admin dashboard
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminAnalytics"
        "401":
          description: Authentication required
        "403":
          description: Super admin access required

  # ── LEADS ─────────────────────────────────────────────────────────────────────
  /leads:
    get:
      operationId: listLeads
      tags: [leads]
      summary: List captured leads (super admin)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Lead"
        "401":
          description: Authentication required
        "403":
          description: Super admin access required
    post:
      operationId: createLead
      tags: [leads]
      summary: Capture a lead from a trial signup, demo request, or proposal request
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLeadInput"
      responses:
        "201":
          description: Lead captured
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Lead"

  # ── COMPANY ───────────────────────────────────────────────────────────────────
  /company:
    get:
      operationId: getMyCompany
      tags: [companies]
      summary: Get the current user's company profile
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Company"
    patch:
      operationId: updateMyCompany
      tags: [companies]
      summary: Update company profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompanyUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Company"

  /companies:
    get:
      operationId: listCompanies
      tags: [companies]
      summary: List all companies (admin)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Company"
    post:
      operationId: createCompany
      tags: [companies]
      summary: Create a company (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompanyInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Company"

  # ── EMPLOYEES ─────────────────────────────────────────────────────────────────
  /company/employees:
    get:
      operationId: listEmployees
      tags: [employees]
      summary: List employees in the current user's company
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Employee"
    post:
      operationId: addEmployee
      tags: [employees]
      summary: Add an employee to the company
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmployeeInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"

  /company/employees/{id}:
    patch:
      operationId: updateEmployee
      tags: [employees]
      summary: Update an employee
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmployeeUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"
    delete:
      operationId: removeEmployee
      tags: [employees]
      summary: Remove an employee from the company
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removed

  # ── PLANS ─────────────────────────────────────────────────────────────────────
  /plans:
    get:
      operationId: listPlans
      tags: [plans]
      summary: List all subscription plans
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Plan"

  # ── SUBSCRIPTIONS ─────────────────────────────────────────────────────────────
  /subscriptions/current:
    get:
      operationId: getCurrentSubscription
      tags: [subscriptions]
      summary: Get the current company subscription
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Subscription"

  /subscriptions/checkout:
    post:
      operationId: createCheckoutSession
      tags: [subscriptions]
      summary: Create a Stripe checkout session for a plan
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CheckoutInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckoutSession"

  /subscriptions/portal:
    post:
      operationId: createCustomerPortalSession
      tags: [subscriptions]
      summary: Create a Stripe customer portal session
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortalSession"

  # ── DASHBOARD ─────────────────────────────────────────────────────────────────
  /dashboard/stats:
    get:
      operationId: getDashboardStats
      tags: [dashboard]
      summary: Get company training dashboard statistics
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardStats"

  /dashboard/employee-progress:
    get:
      operationId: getEmployeeProgress
      tags: [dashboard]
      summary: Get per-employee training progress for HR view
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployeeProgressRow"

  /dashboard/completion-trend:
    get:
      operationId: getCompletionTrend
      tags: [dashboard]
      summary: Monthly completion trend for the last 12 months
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TrendPoint"

  /dashboard/department-participation:
    get:
      operationId: getDepartmentParticipation
      tags: [dashboard]
      summary: Training participation and completion by department
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DepartmentParticipation"

  # ── ENVIRONMENTAL IMPACT ──────────────────────────────────────────────────────
  /impact:
    get:
      operationId: getImpactMetrics
      tags: [impact]
      summary: Get environmental impact metrics for the company
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImpactMetrics"

  /impact/badges:
    get:
      operationId: listBadges
      tags: [impact]
      summary: List earned sustainability badges for the company
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Badge"

  /impact/department-breakdown:
    get:
      operationId: getDepartmentBreakdown
      tags: [impact]
      summary: Training completion breakdown by department
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DepartmentStat"

  # ── ESG (company sustainability) ──────────────────────────────────────────────
  /esg/impact:
    get:
      operationId: getEsgImpact
      tags: [esg]
      summary: Company ESG impact estimated from training engagement
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EsgImpact"

  /esg/score:
    get:
      operationId: getSustainabilityScore
      tags: [esg]
      summary: Company sustainability score, level and recommendations
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SustainabilityScore"

  # ── BLOG ──────────────────────────────────────────────────────────────────────
  /blog:
    get:
      operationId: listBlogPosts
      tags: [blog]
      summary: List published blog posts
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BlogPost"

  /blog/{slug}:
    get:
      operationId: getBlogPost
      tags: [blog]
      summary: Get a blog post by slug
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlogPost"

  # ── TESTIMONIALS ──────────────────────────────────────────────────────────────
  /testimonials:
    get:
      operationId: listTestimonials
      tags: [testimonials]
      summary: List published testimonials for homepage
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Testimonial"

components:
  schemas:
    HealthStatus:
      type: object
      required: [status]
      properties:
        status:
          type: string

    # ── COURSE ────────────────────────────────────────────────────────────────
    Course:
      type: object
      required: [id, title, description, categoryId, durationMinutes, priceUsd, level, isFeatured, thumbnailUrl]
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: string
        categoryId:
          type: integer
        categoryName:
          type: ["string", "null"]
        durationMinutes:
          type: integer
        priceUsd:
          type: number
        level:
          type: string
          enum: [beginner, intermediate, advanced]
        isFeatured:
          type: boolean
        thumbnailUrl:
          type: ["string", "null"]
        previewVideoUrl:
          type: ["string", "null"]
        learningObjectives:
          type: array
          items:
            type: string
        enrollmentCount:
          type: ["integer", "null"]
        rating:
          type: ["number", "null"]
        includesCertificate:
          type: boolean
        passingScore:
          type: integer
        createdAt:
          type: string

    CourseDetail:
      allOf:
        - $ref: "#/components/schemas/Course"
        - type: object
          properties:
            lessons:
              type: array
              items:
                $ref: "#/components/schemas/Lesson"

    CourseInput:
      type: object
      required: [title, description, categoryId, durationMinutes, priceUsd, level]
      properties:
        title:
          type: string
        description:
          type: string
        categoryId:
          type: integer
        durationMinutes:
          type: integer
        priceUsd:
          type: number
        level:
          type: string
          enum: [beginner, intermediate, advanced]
        isFeatured:
          type: boolean
        thumbnailUrl:
          type: ["string", "null"]
        previewVideoUrl:
          type: ["string", "null"]
        learningObjectives:
          type: array
          items:
            type: string
        includesCertificate:
          type: boolean

    CourseUpdate:
      type: object
      properties:
        title:
          type: ["string", "null"]
        description:
          type: ["string", "null"]
        categoryId:
          type: ["integer", "null"]
        durationMinutes:
          type: ["integer", "null"]
        priceUsd:
          type: ["number", "null"]
        level:
          type: ["string", "null"]
        isFeatured:
          type: ["boolean", "null"]
        thumbnailUrl:
          type: ["string", "null"]
        previewVideoUrl:
          type: ["string", "null"]
        learningObjectives:
          type: array
          items:
            type: string
        includesCertificate:
          type: ["boolean", "null"]

    Lesson:
      type: object
      required: [id, courseId, title, orderIndex, durationMinutes]
      properties:
        id:
          type: integer
        courseId:
          type: integer
        title:
          type: string
        orderIndex:
          type: integer
        durationMinutes:
          type: integer
        videoUrl:
          type: ["string", "null"]
        pdfUrl:
          type: ["string", "null"]
        content:
          type: ["string", "null"]

    # ── CATEGORY ──────────────────────────────────────────────────────────────
    Category:
      type: object
      required: [id, name, slug, courseCount]
      properties:
        id:
          type: integer
        name:
          type: string
        slug:
          type: string
        description:
          type: ["string", "null"]
        iconName:
          type: ["string", "null"]
        courseCount:
          type: integer

    # ── ENROLLMENT ────────────────────────────────────────────────────────────
    Enrollment:
      type: object
      required: [id, userId, courseId, status, progressPct, createdAt]
      properties:
        id:
          type: integer
        userId:
          type: string
        courseId:
          type: integer
        courseName:
          type: ["string", "null"]
        courseThumbnail:
          type: ["string", "null"]
        status:
          type: string
          enum: [active, completed, expired]
        progressPct:
          type: integer
        lastAccessedAt:
          type: ["string", "null"]
        completedAt:
          type: ["string", "null"]
        createdAt:
          type: string

    EnrollmentDetail:
      allOf:
        - $ref: "#/components/schemas/Enrollment"
        - type: object
          properties:
            course:
              nullable: true
              allOf:
                - $ref: "#/components/schemas/CourseDetail"

    EnrollmentInput:
      type: object
      required: [courseId]
      properties:
        courseId:
          type: integer

    # ── PROGRESS ──────────────────────────────────────────────────────────────
    LessonProgress:
      type: object
      required: [id, enrollmentId, lessonId, completed]
      properties:
        id:
          type: integer
        enrollmentId:
          type: integer
        lessonId:
          type: integer
        lessonTitle:
          type: ["string", "null"]
        completed:
          type: boolean
        watchedSeconds:
          type: integer
        completedAt:
          type: ["string", "null"]

    ProgressUpdate:
      type: object
      required: [lessonId]
      properties:
        lessonId:
          type: integer
        completed:
          type: boolean
        watchedSeconds:
          type: ["integer", "null"]

    # ── QUIZ ──────────────────────────────────────────────────────────────────
    Quiz:
      type: object
      required: [courseId, questions]
      properties:
        courseId:
          type: integer
        questions:
          type: array
          items:
            $ref: "#/components/schemas/QuizQuestion"

    QuizQuestion:
      type: object
      required: [id, question, options]
      properties:
        id:
          type: integer
        question:
          type: string
        options:
          type: array
          items:
            type: string

    QuizSubmission:
      type: object
      required: [answers]
      properties:
        answers:
          type: array
          items:
            $ref: "#/components/schemas/QuizAnswer"

    QuizAnswer:
      type: object
      required: [questionId, selectedOption]
      properties:
        questionId:
          type: integer
        selectedOption:
          type: integer

    QuizResult:
      type: object
      required: [score, passed, totalQuestions, correctAnswers]
      properties:
        score:
          type: integer
        passed:
          type: boolean
        totalQuestions:
          type: integer
        correctAnswers:
          type: integer
        certificateId:
          type: ["integer", "null"]

    # ── COMMITMENTS ───────────────────────────────────────────────────────────
    CommitmentList:
      type: object
      required: [courseId, commitments]
      properties:
        courseId:
          type: integer
        commitments:
          type: array
          items:
            type: string

    SaveCommitmentsInput:
      type: object
      required: [commitments]
      properties:
        commitments:
          type: array
          items:
            type: string

    # ── PROGRESSION ───────────────────────────────────────────────────────────
    PointsBreakdown:
      type: object
      required: [modulePoints, quizPoints, bonusPoints, totalPoints]
      properties:
        modulePoints:
          type: integer
        quizPoints:
          type: integer
        bonusPoints:
          type: integer
        totalPoints:
          type: integer

    CourseProgressSummary:
      type: object
      required:
        [courseId, modulesCompleted, totalModules, completionPct, quizPassed, badgeEarned, points]
      properties:
        courseId:
          type: integer
        modulesCompleted:
          type: integer
        totalModules:
          type: integer
        completionPct:
          type: integer
        bestScore:
          type: ["integer", "null"]
        quizPassed:
          type: boolean
        certificateId:
          type: ["integer", "null"]
        badgeName:
          type: ["string", "null"]
        badgeEarned:
          type: boolean
        points:
          $ref: "#/components/schemas/PointsBreakdown"

    # ── CERTIFICATE ───────────────────────────────────────────────────────────
    Certificate:
      type: object
      required: [id, userId, courseId, uniqueCode, issuedAt]
      properties:
        id:
          type: integer
        userId:
          type: string
        employeeName:
          type: ["string", "null"]
        companyName:
          type: ["string", "null"]
        courseId:
          type: integer
        courseName:
          type: ["string", "null"]
        uniqueCode:
          type: string
        issuedAt:
          type: string
        pdfUrl:
          type: ["string", "null"]

    # ── LEARNING PATHS ──────────────────────────────────────────────────────────
    LearningPathModule:
      type: object
      required: [courseId, courseTitle, durationMinutes, level, orderIndex, completed]
      properties:
        courseId:
          type: integer
        courseTitle:
          type: string
        durationMinutes:
          type: integer
        level:
          type: string
        orderIndex:
          type: integer
        completed:
          type: boolean
    LearningPath:
      type: object
      required: [id, slug, title, description, audience, icon, totalModules, completedModules, progressPct, totalMinutes, modules]
      properties:
        id:
          type: integer
        slug:
          type: string
        title:
          type: string
        description:
          type: string
        audience:
          type: string
        icon:
          type: string
        totalModules:
          type: integer
        completedModules:
          type: integer
        progressPct:
          type: integer
        totalMinutes:
          type: integer
        modules:
          type: array
          items:
            $ref: "#/components/schemas/LearningPathModule"

    # ── BADGES ──────────────────────────────────────────────────────────────────
    AchievementBadge:
      type: object
      required: [id, slug, name, description, icon, earned, earnedAt, progressCurrent, progressTarget]
      properties:
        id:
          type: integer
        slug:
          type: string
        name:
          type: string
        description:
          type: string
        icon:
          type: string
        earned:
          type: boolean
        earnedAt:
          type: ["string", "null"]
        progressCurrent:
          type: integer
        progressTarget:
          type: integer

    LeaderboardEntry:
      type: object
      required: [rank, employeeId, name, value, valueLabel]
      properties:
        rank:
          type: integer
        employeeId:
          type: integer
        name:
          type: string
        department:
          type: ["string", "null"]
        value:
          type: integer
        valueLabel:
          type: string

    Leaderboard:
      type: object
      required: [key, title, description, unit, entries]
      properties:
        key:
          type: string
        title:
          type: string
        description:
          type: string
        unit:
          type: string
        entries:
          type: array
          items:
            $ref: "#/components/schemas/LeaderboardEntry"

    LeaderboardResponse:
      type: object
      required: [enabled, boards]
      properties:
        enabled:
          type: boolean
        boards:
          type: array
          items:
            $ref: "#/components/schemas/Leaderboard"

    Challenge:
      type: object
      required:
        [id, slug, title, description, icon, theme, focus, unit, goalTarget, points, startDate, endDate, status, joined, progress, completed, pointsEarned, progressPct]
      properties:
        id:
          type: integer
        slug:
          type: string
        title:
          type: string
        description:
          type: string
        icon:
          type: string
        theme:
          type: string
        focus:
          type: string
        unit:
          type: string
        goalTarget:
          type: integer
        points:
          type: integer
        badgeName:
          type: ["string", "null"]
        startDate:
          type: string
        endDate:
          type: string
        status:
          type: string
          enum: [upcoming, active, ended]
        joined:
          type: boolean
        progress:
          type: integer
        completed:
          type: boolean
        pointsEarned:
          type: integer
        progressPct:
          type: integer

    ChallengeListResponse:
      type: object
      required: [totalPoints, completedCount, challenges]
      properties:
        totalPoints:
          type: integer
        completedCount:
          type: integer
        challenges:
          type: array
          items:
            $ref: "#/components/schemas/Challenge"

    ChallengeProgressInput:
      type: object
      properties:
        amount:
          type: integer
          minimum: 1

    # ── COMPLIANCE ─────────────────────────────────────────────────────────────
    ComplianceAssignment:
      type: object
      required:
        [id, employeeId, employeeName, courseId, courseTitle, isMandatory, assignedAt, status]
      properties:
        id:
          type: integer
        employeeId:
          type: integer
        employeeName:
          type: string
        department:
          type: ["string", "null"]
        courseId:
          type: integer
        courseTitle:
          type: string
        isMandatory:
          type: boolean
        assignedAt:
          type: string
        dueDate:
          type: ["string", "null"]
        completedAt:
          type: ["string", "null"]
        expiresAt:
          type: ["string", "null"]
        status:
          type: string
          enum: [compliant, expiring_soon, expired, overdue, not_started]

    ComplianceSummary:
      type: object
      required:
        [total, compliant, expiringSoon, expired, overdue, notStarted, complianceRate]
      properties:
        total:
          type: integer
        compliant:
          type: integer
        expiringSoon:
          type: integer
        expired:
          type: integer
        overdue:
          type: integer
        notStarted:
          type: integer
        complianceRate:
          type: integer

    ComplianceCourseStat:
      type: object
      required: [courseId, title, total, compliant, complianceRate]
      properties:
        courseId:
          type: integer
        title:
          type: string
        total:
          type: integer
        compliant:
          type: integer
        complianceRate:
          type: integer

    ComplianceOverview:
      type: object
      required: [summary, assignments, courses]
      properties:
        summary:
          $ref: "#/components/schemas/ComplianceSummary"
        assignments:
          type: array
          items:
            $ref: "#/components/schemas/ComplianceAssignment"
        courses:
          type: array
          items:
            $ref: "#/components/schemas/ComplianceCourseStat"

    AssignCourseInput:
      type: object
      required: [courseId]
      properties:
        courseId:
          type: integer
        employeeIds:
          type: array
          items:
            type: integer
        department:
          type: string
        dueDate:
          type: string

    AssignCourseResult:
      type: object
      required: [assigned, skipped]
      properties:
        assigned:
          type: integer
        skipped:
          type: integer

    TrainingReminder:
      type: object
      required: [id, companyId, employeeId, type, message, createdAt]
      properties:
        id:
          type: integer
        companyId:
          type: integer
        employeeId:
          type: integer
        courseId:
          type: ["integer", "null"]
        type:
          type: string
        message:
          type: string
        createdAt:
          type: string

    SendReminderInput:
      type: object
      required: [employeeId]
      properties:
        employeeId:
          type: integer
        courseId:
          type: integer
        type:
          type: string
          enum: [reminder, retraining]
        message:
          type: string

    BulkImportRow:
      type: object
      required: [name, email]
      properties:
        name:
          type: string
        email:
          type: string
        department:
          type: string
        role:
          type: string

    BulkImportInput:
      type: object
      required: [employees]
      properties:
        employees:
          type: array
          items:
            $ref: "#/components/schemas/BulkImportRow"

    BulkImportResult:
      type: object
      required: [created, skipped, errors]
      properties:
        created:
          type: integer
        skipped:
          type: integer
        errors:
          type: array
          items:
            type: string

    RetrainingScanResult:
      type: object
      required: [notified, skipped]
      properties:
        notified:
          type: integer
        skipped:
          type: integer

    # ── COMPANY ───────────────────────────────────────────────────────────────
    Company:
      type: object
      required: [id, name, slug, planId, employeeCount, createdAt]
      properties:
        id:
          type: integer
        name:
          type: string
        slug:
          type: string
        industry:
          type: ["string", "null"]
        logoUrl:
          type: ["string", "null"]
        planId:
          type: ["integer", "null"]
        planName:
          type: ["string", "null"]
        employeeCount:
          type: integer
        maxEmployees:
          type: ["integer", "null"]
        completionRate:
          type: ["number", "null"]
        certificatesIssued:
          type: ["integer", "null"]
        badges:
          type: array
          items:
            type: string
        isPublicProfile:
          type: boolean
        leaderboardEnabled:
          type: boolean
        createdAt:
          type: string

    CompanyInput:
      type: object
      required: [name, industry]
      properties:
        name:
          type: string
        industry:
          type: string
        logoUrl:
          type: ["string", "null"]

    CompanyUpdate:
      type: object
      properties:
        name:
          type: ["string", "null"]
        industry:
          type: ["string", "null"]
        logoUrl:
          type: ["string", "null"]
        isPublicProfile:
          type: ["boolean", "null"]
        leaderboardEnabled:
          type: ["boolean", "null"]

    # ── EMPLOYEE ──────────────────────────────────────────────────────────────
    Employee:
      type: object
      required: [id, companyId, email, name, role, createdAt]
      properties:
        id:
          type: integer
        companyId:
          type: integer
        clerkUserId:
          type: ["string", "null"]
        email:
          type: string
        name:
          type: string
        department:
          type: ["string", "null"]
        role:
          type: string
          enum: [employee, manager, admin]
        enrolledCourses:
          type: ["integer", "null"]
        completedCourses:
          type: ["integer", "null"]
        certificates:
          type: ["integer", "null"]
        lastActiveAt:
          type: ["string", "null"]
        createdAt:
          type: string

    EmployeeInput:
      type: object
      required: [email, name]
      properties:
        email:
          type: string
        name:
          type: string
        department:
          type: ["string", "null"]
        role:
          type: string
          enum: [employee, manager, admin]

    EmployeeUpdate:
      type: object
      properties:
        name:
          type: ["string", "null"]
        department:
          type: ["string", "null"]
        role:
          type: ["string", "null"]

    # ── PLANS ─────────────────────────────────────────────────────────────────
    Plan:
      type: object
      required: [id, name, maxEmployees, priceMonthly, priceAnnual, features]
      properties:
        id:
          type: integer
        name:
          type: string
        slug:
          type: string
        maxEmployees:
          type: ["integer", "null"]
        priceMonthly:
          type: number
        priceAnnual:
          type: number
        features:
          type: array
          items:
            type: string
        isPopular:
          type: boolean
        stripePriceIdMonthly:
          type: ["string", "null"]
        stripePriceIdAnnual:
          type: ["string", "null"]

    # ── SUBSCRIPTION ──────────────────────────────────────────────────────────
    Subscription:
      type: object
      required: [id, companyId, planId, status, currentPeriodEnd]
      properties:
        id:
          type: integer
        companyId:
          type: integer
        planId:
          type: integer
        planName:
          type: ["string", "null"]
        status:
          type: string
          enum: [active, trialing, past_due, canceled, incomplete]
        currentPeriodStart:
          type: ["string", "null"]
        currentPeriodEnd:
          type: string
        stripeSubscriptionId:
          type: ["string", "null"]
        cancelAtPeriodEnd:
          type: boolean

    CheckoutInput:
      type: object
      required: [planId, billingInterval]
      properties:
        planId:
          type: integer
        billingInterval:
          type: string
          enum: [monthly, annual]

    CheckoutSession:
      type: object
      required: [url]
      properties:
        url:
          type: string

    PortalSession:
      type: object
      required: [url]
      properties:
        url:
          type: string

    # ── DASHBOARD ─────────────────────────────────────────────────────────────
    DashboardStats:
      type: object
      required: [totalEmployees, activeEmployees, completionRate, certificatesIssued, coursesAssigned, coursesCompleted, avgScore, learningHoursCompleted, trainingAdoptionRate, employeesNeedingRetraining]
      properties:
        totalEmployees:
          type: integer
        activeEmployees:
          type: integer
        completionRate:
          type: number
        certificatesIssued:
          type: integer
        coursesAssigned:
          type: integer
        coursesCompleted:
          type: integer
        avgScore:
          type: number
        learningHoursCompleted:
          type: number
        trainingAdoptionRate:
          type: number
        employeesNeedingRetraining:
          type: integer

    EmployeeProgressRow:
      type: object
      required: [employeeId, employeeName, email, department, coursesCompleted, totalCourses, completionRate, lastActiveAt]
      properties:
        employeeId:
          type: integer
        employeeName:
          type: string
        email:
          type: string
        department:
          type: ["string", "null"]
        coursesCompleted:
          type: integer
        totalCourses:
          type: integer
        completionRate:
          type: number
        certificates:
          type: integer
        avgScore:
          type: integer
        lastActiveAt:
          type: ["string", "null"]
        needsRetraining:
          type: boolean

    TrendPoint:
      type: object
      required: [month, completionRate, adoptionRate, activeLearners]
      properties:
        month:
          type: string
        completionRate:
          type: integer
        adoptionRate:
          type: integer
        activeLearners:
          type: integer

    MonthlyTrendPoint:
      type: object
      required: [month, completions, enrollments]
      properties:
        month:
          type: string
        completions:
          type: integer
        enrollments:
          type: integer

    DepartmentParticipation:
      type: object
      required: [department, employees, participationRate, completionRate]
      properties:
        department:
          type: string
        employees:
          type: integer
        participationRate:
          type: integer
        completionRate:
          type: integer

    # ── ENVIRONMENTAL IMPACT ──────────────────────────────────────────────────
    ImpactMetrics:
      type: object
      required: [employeesTrained, coursesCompleted, certificatesIssued, companiesParticipating, lessonsCompleted, treesPlanted, totalDonated, co2SequesteredKg, areaReforestedM2, donationRatePct, treeCost, partnerName]
      properties:
        employeesTrained:
          type: integer
        coursesCompleted:
          type: integer
        certificatesIssued:
          type: integer
        companiesParticipating:
          type: integer
        lessonsCompleted:
          type: integer
        treesPlanted:
          type: integer
        totalDonated:
          type: number
        co2SequesteredKg:
          type: number
        areaReforestedM2:
          type: number
        donationRatePct:
          type: number
        treeCost:
          type: number
        partnerName:
          type: string
        topContributors:
          type: array
          items:
            $ref: "#/components/schemas/ContributorStat"
        monthlyTrend:
          type: array
          items:
            $ref: "#/components/schemas/MonthlyTrendPoint"

    ContributorStat:
      type: object
      required: [company, trees, donated]
      properties:
        company:
          type: string
        trees:
          type: integer
        donated:
          type: number

    Badge:
      type: object
      required: [id, name, slug, description, awardedAt]
      properties:
        id:
          type: integer
        name:
          type: string
        slug:
          type: string
        description:
          type: string
        iconUrl:
          type: ["string", "null"]
        awardedAt:
          type: string

    DepartmentStat:
      type: object
      required: [department, employeeCount, completionRate, certificates]
      properties:
        department:
          type: string
        employeeCount:
          type: integer
        completionRate:
          type: number
        certificates:
          type: integer

    # ── ESG (company sustainability) ──────────────────────────────────────────
    EsgImpact:
      type: object
      required: [co2EquivalentKg, treesEquivalent, wasteDivertedKg, recyclingParticipation, plasticReductionScore, waterSavingsScore, carbonAwarenessScore, sustainabilityEngagementScore]
      properties:
        co2EquivalentKg:
          type: integer
        treesEquivalent:
          type: integer
        wasteDivertedKg:
          type: integer
        recyclingParticipation:
          type: integer
        plasticReductionScore:
          type: integer
        waterSavingsScore:
          type: integer
        carbonAwarenessScore:
          type: integer
        sustainabilityEngagementScore:
          type: integer

    ScoreComponent:
      type: object
      required: [label, value]
      properties:
        label:
          type: string
        value:
          type: integer

    SustainabilityScore:
      type: object
      required: [score, level, pointsToNextLevel, components, recommendations]
      properties:
        score:
          type: integer
        level:
          type: string
        nextLevel:
          type: ["string", "null"]
        pointsToNextLevel:
          type: integer
        components:
          type: array
          items:
            $ref: "#/components/schemas/ScoreComponent"
        recommendations:
          type: array
          items:
            type: string

    # ── BLOG ──────────────────────────────────────────────────────────────────
    BlogPost:
      type: object
      required: [id, title, slug, excerpt, content, authorName, publishedAt, tags]
      properties:
        id:
          type: integer
        title:
          type: string
        slug:
          type: string
        excerpt:
          type: string
        content:
          type: string
        authorName:
          type: string
        thumbnailUrl:
          type: ["string", "null"]
        publishedAt:
          type: string
        tags:
          type: array
          items:
            type: string

    # ── TESTIMONIAL ───────────────────────────────────────────────────────────
    Testimonial:
      type: object
      required: [id, authorName, authorTitle, company, text, rating]
      properties:
        id:
          type: integer
        authorName:
          type: string
        authorTitle:
          type: string
        company:
          type: string
        text:
          type: string
        rating:
          type: integer
        avatarUrl:
          type: ["string", "null"]

    # ── LEADS ─────────────────────────────────────────────────────────────────
    Lead:
      type: object
      required: [id, name, email, companyName, interest, status, createdAt]
      properties:
        id:
          type: integer
        name:
          type: string
        email:
          type: string
        companyName:
          type: string
        phone:
          type: ["string", "null"]
        industry:
          type: ["string", "null"]
        employeeRange:
          type: ["string", "null"]
        interest:
          type: string
          enum: [trial, demo, proposal]
        message:
          type: ["string", "null"]
        planId:
          type: ["integer", "null"]
        status:
          type: string
        createdAt:
          type: string

    CreateLeadInput:
      type: object
      required: [name, email, companyName, interest]
      properties:
        name:
          type: string
        email:
          type: string
        companyName:
          type: string
        phone:
          type: string
        industry:
          type: string
        employeeRange:
          type: string
        interest:
          type: string
          enum: [trial, demo, proposal]
        message:
          type: string
        planId:
          type: integer

    # ── ADMIN ANALYTICS ─────────────────────────────────────────────────────────
    AdminAnalytics:
      type: object
      required: [companiesRegistered, activeCompanies, annualRevenue, currency, trialSignups, convertedTrials, trialConversionRate, leadsByInterest, totalEmployees, activeEmployees, engagementRate, certificatesIssued, totalEnrollments, completedEnrollments, overallCompletionRate, popularCourses]
      properties:
        companiesRegistered:
          type: integer
        activeCompanies:
          type: integer
        annualRevenue:
          type: number
        currency:
          type: string
        trialSignups:
          type: integer
        convertedTrials:
          type: integer
        trialConversionRate:
          type: integer
        leadsByInterest:
          $ref: "#/components/schemas/LeadsByInterest"
        totalEmployees:
          type: integer
        activeEmployees:
          type: integer
        engagementRate:
          type: integer
        certificatesIssued:
          type: integer
        totalEnrollments:
          type: integer
        completedEnrollments:
          type: integer
        overallCompletionRate:
          type: integer
        popularCourses:
          type: array
          items:
            $ref: "#/components/schemas/PopularCourse"

    LeadsByInterest:
      type: object
      required: [trial, demo, proposal]
      properties:
        trial:
          type: integer
        demo:
          type: integer
        proposal:
          type: integer

    PopularCourse:
      type: object
      required: [courseId, title, enrollments, completionRate]
      properties:
        courseId:
          type: ["integer", "null"]
        title:
          type: string
        enrollments:
          type: integer
        completionRate:
          type: integer
